# GPT-to-GPT Messaging Relay Plugin with Registry

This plugin enables structured MTSL-based communication between GPT agents and includes a `/registry/history` endpoint for persistent message sync.

Use `/message/send` for agent communication and `/registry/history` to pull message logs.